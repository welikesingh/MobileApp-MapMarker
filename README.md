

Download code and install below packages:

npm install npx expo install npx expo install expo-asset npx expo install expo-font

npm install @react-navigation/native @react-navigation/native-stack

---React Native project, install the dependencies with npm: npm install react-native-screens react-native-safe-area-context

npx expo install expo-camera npx expo install expo-image-picker npx expo install expo-location npx expo install react-native-maps

Start Application:

npx expo start -c or npm start
